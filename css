.chat-container {
  display: flex;
  height: 100vh;
}

.chat-menu {
  width: 75px;
  background-color: #000000;
  /* Add styles for menu icons */
}

.chat-content {
  flex: 1;
  display: flex;
  flex-direction: column;
}

.search-bar {
  /* Style your search bar */
}

.messages {
  flex: 1;
  overflow-y: auto;
}

.message {
  display: flex;
  align-items: center;
  padding: 10px;
}

.received {
  justify-content: flex-start;
}

.sent {
  justify-content: flex-end;
}

.message img {
  border-radius: 50%;
  margin: 0 10px;
}

.message-info p {
  margin: 0;
  padding: 10px;
  border-radius: 20px;
  background-color: #f0f0f0;
}

.chat-input {
  border-top: 1px solid #ccc;
  padding: 10px;
}

.chat-input input {
  width: 100%;
  padding: 10px;
  border-radius: 20px;
  border: 1px solid #ccc;
}

/* You should continue to fill in the styles based on the visual design of the chat interface */
